// Re-export auth context and hooks for easy importing
export { AuthProvider, useAuth, withAuth } from '../contexts/AuthContext';
export type { User, AuthContextType } from '../contexts/AuthContext';
